export class Language {
    code:string;
    flag:string;
    name:string;

    constructor(code: string, flag: string, name: string) {
        this.code = code;
        this.flag = flag;
        this.name = name;
    }
}
